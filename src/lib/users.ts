// src/lib/analytics.ts (formerly users.ts)
/*
export function trackEvent(name: string, params: any) {
  logEvent(analytics, name, params);
}

logEvent(analytics, 'story_started',       { story_id, category, age_group });
logEvent(analytics, 'story_completed',     { story_id, duration_seconds });
logEvent(analytics, 'mission_completed',   { story_id, profile_id });
logEvent(analytics, 'ai_story_generated',  { age_group, language, theme, model: 'gemini-1.5-flash' });
logEvent(analytics, 'ai_quota_reached',    { plan, uid });
logEvent(analytics, 'plan_viewed',         { plan_id });
logEvent(analytics, 'purchase',            { plan_id, value, currency: 'BRL' });
logEvent(analytics, 'pwa_installed',       { platform });
logEvent(analytics, 'tts_generated',       { language, age_group, duration_chars });
*/
